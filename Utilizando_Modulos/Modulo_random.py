import random # importa o modulo random que podem gerar qualquer valor aleatorio.

n1 = random.randint(0, 100)  # Gera um valor entre dois números definidos
n2 = random.random()  # Gera um valor aleatorio entre 1 e 0

print(n1, n2)
